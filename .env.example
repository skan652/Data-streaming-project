# ─────────────────────────────────────────────────────────────────────────────
# .env.example  –  copy to .env and fill in YOUR values before running.
# .env is gitignored; this file is safe to commit.
# ─────────────────────────────────────────────────────────────────────────────

# ── OpenWeather API ──────────────────────────────────────────────────────────
# Get a free key at https://openweathermap.org/api
OPENWEATHER_API_KEY=your_openweather_api_key_here

# City to fetch weather data for
WEATHER_CITY=Tunis

# How often (seconds) the producer polls the API
FETCH_INTERVAL_SEC=60

# ── Kafka ────────────────────────────────────────────────────────────────────
# Use "kafka:9092" inside Docker Compose; "localhost:29092" for local dev
KAFKA_BOOTSTRAP_SERVERS=localhost:29092

# ── Spark / ML ───────────────────────────────────────────────────────────────
# Paths inside the container (set automatically by docker-compose.yaml)
MODEL_PATH=/opt/spark-apps/model/temperature_model.pkl
ANOMALY_MODEL_PATH=/opt/spark-apps/model/anomaly_detector.pkl
OUTPUT_PATH=/opt/spark-apps/output

# ── Model training – real historical data (Open-Meteo archive API) ───────────
# Coordinates of the city to train on (defaults to Tunis)
TRAINING_LATITUDE=36.8065
TRAINING_LONGITUDE=10.1815
# Date range for the historical fetch (ISO-8601, inclusive)
TRAINING_START_DATE=2022-01-01
TRAINING_END_DATE=2023-12-31
